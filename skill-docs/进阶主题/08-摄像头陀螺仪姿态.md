# MAML 背屏主题 · 摄像头陀螺仪姿态计算

> 作者：唯梦倾城 | 来源：深渊主题 manifest.xml 分析

---

## 一、概述

并不是读取系统相机 API，而是**通过陀螺仪传感器推算出设备姿态，再把姿态映射成摄像头旋转向量**，传给 GL shader 做"避开摄像头/跟随镜头"视觉效果。

---

## 二、传感器入口

```xml
<SensorBinder name="sensor" rate="0" threshold="0.05" type="gyroscope">
    <Variable name="gyro_x" index="0"/>
    <Variable name="gyro_y" index="1"/>
    <Variable name="gyro_z" index="2"/>
    <Trigger>
        <FunctionCommand target="_calc_camera"/>
    </Trigger>
</SensorBinder>
```

每次陀螺仪有变化，就触发 `_calc_camera`，进入姿态计算流程。

---

## 三、摄像头向量初始化

```xml
<Var name="#camera_x" expression="0" const="true"/>
<Var name="#camera_y" expression="0" const="true"/>
<Var name="#camera_z" expression="0" const="true"/>
```

---

## 四、姿态计算流程

真正更新发生在 `_physics_process` 中：

```xml
<VariableCommand name="camera_x" expression="#quatToEuler_res[0]"/>
<VariableCommand name="camera_y" expression="#quatToEuler_res[1]*-1"/>
<VariableCommand name="camera_z" expression="#quatToEuler_res[2]"/>
```

完整流程：
1. 读取陀螺仪 `gyro_x / gyro_y / gyro_z`
2. 平滑
3. 积分得到 `basis_world`
4. 转四元数 `q_world`
5. 与锚点四元数 `q_anchor` 求差 `q_diff`
6. 把 `q_diff` 转成欧拉角，得到 `camera_x / camera_y / camera_z`

---

## 五、传给渲染层

```xml
<GLCommand name="mRectMaterial" command="update" target="glView" type="RenderMaterial">
    <Uniform name="CameraVectorRotation" type="float3" value="#camera_x,#camera_y,#camera_z"/>
</GLCommand>
```

圆环和文字材质也会同步更新同一个 uniform。

---

## 六、"摄像头位置"与圆环的关系

分两层：

1. **布局锚点（静态机型位）**
   - `circleTfPosX / circleTfPosY` 按机型分支出圆环基础位置
   - 通过 `GLCommand setLocalPosition` 写到 `circleTf`

```xml
<Var name="circleTfPosX" const="true" expression="ifelse(#isP2,-1.14,-1.05)"/>
<Var name="circleTfPosY" const="true" expression="ifelse(#isP2,0.514,0.478)"/>
<GLCommand name="circleTf" target="glView" command="setLocalPosition" params="#circleTfPosX,#circleTfPosY,0"/>
```

2. **姿态驱动（动态镜头位）**
   - 陀螺仪算出的 `camera_x/y/z` 不直接改 `circleTfPosX/Y`
   - 而是作为 `CameraVectorRotation` 传入材质，驱动 shader 里的光照/视角变化

**结论：圆环中心点是机型参数定死的，圆环"跟着动"的视觉来自材质旋转向量，而不是直接改坐标。**

---

## 七、传感器生命周期管理

```xml
<!-- 进入 AOD/暂停时关闭传感器 -->
<Function name="_stop_sensor">
    <SensorCommand target="sensor" command="turnOff"/>
    <VariableCommand name="sensor_enable" expression="0"/>
</Function>

<!-- 恢复时重新开启 -->
<Function name="_start_sensor">
    <FunctionCommand target="_clear_camera_data"/>
    <VariableCommand name="_time_last" expression="#time_sys"/>
    <SensorCommand target="sensor" command="turnOn"/>
    <VariableCommand name="sensor_enable" expression="1"/>
    <AnimationCommand target="_ticker" command="play" tags="loop" delay="20"/>
</Function>
```

---

## 八、摄像机渲染参数

```xml
<Var name="rect_render_plane_z_aod" const="true" expression="ifelse(#isP2,-0.35,-0.37)" type="number"/>
<Var name="text_render_plane_z_init" const="true" expression="1.56" type="number"/>
<Var name="text_render_plane_z_aod" const="true" expression="1.08" type="number"/>
<Var name="text_fog_start_z_init" const="true" expression="3.0" type="number"/>
<Var name="text_fog_start_z_aod" const="true" expression="1.08" type="number"/>
```
